# skill_lab

A simple Python skill lab demonstrating data partitioning and user management.

## Usage

```bash
python program.py
```

## Features

- Initialize partitions and assign users to them
- Add users to partitions with the `add_user` helper
- Retrieve all users across partitions with `get_all_users`
